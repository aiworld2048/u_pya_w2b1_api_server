<div class="card shadow rounded">
                <div class="card-header bg-light border-bottom-0">
                    <h5 class="mb-0">
                        <i class="fas fa-users"></i> 
                        @if(Auth::user()->type === '10')
                            Combined Agent Report Summary
                        @else
                            Combined Player Report Summary
                        @endif
                    </h5>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table id="mytable" class="table table-bordered table-hover mb-0">
                            <thead class="thead-light">
                                <tr>
                                    @if(Auth::user()->type === '10')
                                        <th>Agent Name</th>
                                    @else
                                        <th>Player ID</th>
                                        <th>Agent Name</th>
                                    @endif
                                    <th class="text-right">Total Bet</th>
                                    <th class="text-right">Total Win</th>
                                    <th class="text-right">Total Lose</th>
                                    <th class="text-right">Net Win/Lose</th>
                                </tr>
                            </thead>
                            <tbody>
                                @forelse($report as $row)
                                    <tr>
                                        @if(Auth::user()->type === '10')
                                            <td><strong>{{ $row->agent_name ?? 'N/A' }}</strong></td>
                                        @else
                                            <td><strong>{{ $row->member_account ?? 'N/A' }}</strong></td>
                                            <td>{{ $row->agent_name ?? 'N/A' }}</td>
                                        @endif
                                        <td class="text-right">
                                            <span class="badge badge-info">{{ number_format($row->total_bet, 2) }}</span>
                                        </td>
                                        <td class="text-right">
                                            <span class="badge badge-success">{{ number_format($row->total_win, 2) }}</span>
                                        </td>
                                        <td class="text-right">
                                            <span class="badge badge-danger">{{ number_format($row->total_lose, 2) }}</span>
                                        </td>
                                        <td class="text-right">
                                            @php
                                                $netWinLoss = $row->total_win - $row->total_bet;
                                            @endphp
                                            @if($netWinLoss > 0)
                                                <span class="text-success font-weight-bold">
                                                    <i class="fas fa-arrow-up"></i> +{{ number_format($netWinLoss, 2) }}
                                                </span>
                                            @elseif($netWinLoss < 0)
                                                <span class="text-danger font-weight-bold">
                                                    <i class="fas fa-arrow-down"></i> {{ number_format($netWinLoss, 2) }}
                                                </span>
                                            @else
                                                <span class="text-muted">0.00</span>
                                            @endif
                                        </td>
                                    </tr>
                                @empty
                                    <tr>
                                        <td colspan="{{ Auth::user()->type === '10' ? '5' : '6' }}" class="text-center py-5">
                                            <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                                            <p class="text-muted">No combined data found for the selected date range.</p>
                                        </td>
                                    </tr>
                                @endforelse
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>

            ---------------
            <div class="card shadow rounded">
                <div class="card-header bg-dark text-white">
                    <h5 class="mb-0"><i class="fas fa-calculator"></i> Summary Totals</h5>
                </div>
                <div class="card-body">
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><i class="fas fa-coins text-info"></i> Total Bet Amount:</span>
                            <strong class="text-dark">{{ number_format($total['totalBet'], 2) }}</strong>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><i class="fas fa-trophy text-success"></i> Total Win Amount:</span>
                            <strong class="text-success">{{ number_format($total['totalWin'], 2) }}</strong>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><i class="fas fa-times-circle text-danger"></i> Total Lose Amount:</span>
                            <strong class="text-danger">{{ number_format($total['totalLose'], 2) }}</strong>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center bg-light">
                            <span><i class="fas fa-balance-scale"></i> Net Win/Lose:</span>
                            @if($total['netWinLoss'] > 0)
                                <strong class="text-success">
                                    <i class="fas fa-arrow-up"></i> +{{ number_format($total['netWinLoss'], 2) }}
                                </strong>
                            @elseif($total['netWinLoss'] < 0)
                                <strong class="text-danger">
                                    <i class="fas fa-arrow-down"></i> {{ number_format($total['netWinLoss'], 2) }}
                                </strong>
                            @else
                                <strong class="text-muted">0.00</strong>
                            @endif
                        </li>
                    </ul>
                </div>
            </div>